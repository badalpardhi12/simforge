<?xml version="1.0"?>
<!--
Valid8 Robot Cell Controlled Description

This XACRO file extends the valid8_cell description by adding ros2_control
hardware interface configuration for the UR Robot Driver.

It includes:
- Complete Valid8 cell description (workcell + robot)
- ur.ros2_control.xacro for hardware interface configuration
- Support for real hardware or mock hardware simulation

This file accepts the same parameters as ur.urdf.xacro from ur_description
so it can be used with ur_control.launch.py from ur_robot_driver.

Based on Universal Robots ROS 2 custom workcell tutorial.
-->
<robot name="valid8_cell" xmlns:xacro="http://wiki.ros.org/xacro">
  
  <!-- Include UR robot macro from ur_description -->
  <xacro:include filename="$(find ur_description)/urdf/ur_macro.xacro"/>
  
  <!-- Include Valid8 cell macro -->
  <xacro:include filename="$(find valid8_cell_description)/urdf/valid8_cell_macro.xacro"/>
  
  <!-- Include ros2_control macro for UR robot -->
  <xacro:include filename="$(find ur_description)/urdf/ur.ros2_control.xacro"/>

  <!-- ========== Arguments (matching ur.urdf.xacro) ========== -->
  
  <!-- Robot type -->
  <xacro:arg name="name" default="ur"/>
  
  <!-- TF prefix for multi-robot support -->
  <xacro:arg name="tf_prefix" default=""/>
  
  <!-- Robot IP for real hardware -->
  <xacro:arg name="robot_ip" default="0.0.0.0"/>
  
  <!-- Configuration files from UR driver -->
  <xacro:arg name="joint_limit_params" default=""/>
  <xacro:arg name="kinematics_params" default=""/>
  <xacro:arg name="physical_params" default=""/>
  <xacro:arg name="visual_params" default=""/>
  
  <!-- Safety settings -->
  <xacro:arg name="safety_limits" default="false"/>
  <xacro:arg name="safety_pos_margin" default="0.15"/>
  <xacro:arg name="safety_k_position" default="20"/>
  
  <!-- RTDE configuration files -->
  <xacro:arg name="script_filename" default=""/>
  <xacro:arg name="input_recipe_filename" default=""/>
  <xacro:arg name="output_recipe_filename" default=""/>
  
  <!-- Mock hardware for simulation -->
  <xacro:arg name="use_fake_hardware" default="false"/>
  <xacro:arg name="fake_sensor_commands" default="false"/>
  
  <!-- Headless mode - run without URCap on teach pendant -->
  <xacro:arg name="headless_mode" default="true"/>
  
  <!-- Tool communication settings -->
  <xacro:arg name="use_tool_communication" default="false"/>
  <xacro:arg name="tool_voltage" default="0"/>
  <xacro:arg name="tool_parity" default="0"/>
  <xacro:arg name="tool_baud_rate" default="115200"/>
  <xacro:arg name="tool_stop_bits" default="1"/>
  <xacro:arg name="tool_rx_idle_chars" default="1.5"/>
  <xacro:arg name="tool_tx_idle_chars" default="3.5"/>
  <xacro:arg name="tool_device_name" default="/tmp/ttyUR"/>
  <xacro:arg name="tool_tcp_port" default="54321"/>
  
  <!-- Network settings -->
  <xacro:arg name="reverse_ip" default="0.0.0.0"/>
  <xacro:arg name="script_command_port" default="50004"/>
  <xacro:arg name="reverse_port" default="50001"/>
  <xacro:arg name="script_sender_port" default="50002"/>
  <xacro:arg name="trajectory_port" default="50003"/>

  <!-- ========== World Link ========== -->
  <link name="world"/>

  <!-- ========== Valid8 Workcell ========== -->
  <!-- Creates: shop_floor, table_link, face_link, robot_mount -->
  <xacro:valid8_cell parent="world">
    <origin xyz="0 0 0" rpy="0 0 0"/>
  </xacro:valid8_cell>

  <!-- ========== UR Robot ========== -->
  <!-- The robot is attached to robot_mount link -->
  <xacro:ur_robot
    name="$(arg name)"
    tf_prefix="$(arg tf_prefix)"
    parent="robot_mount"
    joint_limits_parameters_file="$(arg joint_limit_params)"
    kinematics_parameters_file="$(arg kinematics_params)"
    physical_parameters_file="$(arg physical_params)"
    visual_parameters_file="$(arg visual_params)"
    safety_limits="$(arg safety_limits)"
    safety_pos_margin="$(arg safety_pos_margin)"
    safety_k_position="$(arg safety_k_position)"
    use_fake_hardware="$(arg use_fake_hardware)"
    fake_sensor_commands="$(arg fake_sensor_commands)"
    headless_mode="$(arg headless_mode)"
    robot_ip="$(arg robot_ip)"
    script_filename="$(arg script_filename)"
    output_recipe_filename="$(arg output_recipe_filename)"
    input_recipe_filename="$(arg input_recipe_filename)"
  >
    <!-- Robot base is directly on the mount (no additional offset) -->
    <origin xyz="0 0 0" rpy="0 0 0"/>
  </xacro:ur_robot>

  <!-- ========== ros2_control Hardware Interface ========== -->
  <!-- Load kinematics hash from params file -->
  <xacro:property name="kinematics_params_file" value="$(arg kinematics_params)"/>
  <xacro:property name="kinematics_params_data" value="${xacro.load_yaml(kinematics_params_file)}"/>
  
  <xacro:ur_ros2_control
    name="$(arg name)"
    tf_prefix="$(arg tf_prefix)"
    hash_kinematics="${kinematics_params_data['kinematics']['hash'] if 'hash' in kinematics_params_data.get('kinematics', {}) else ''}"
    robot_ip="$(arg robot_ip)"
    script_filename="$(arg script_filename)"
    output_recipe_filename="$(arg output_recipe_filename)"
    input_recipe_filename="$(arg input_recipe_filename)"
    use_fake_hardware="$(arg use_fake_hardware)"
    fake_sensor_commands="$(arg fake_sensor_commands)"
    headless_mode="$(arg headless_mode)"
    use_tool_communication="$(arg use_tool_communication)"
    tool_voltage="$(arg tool_voltage)"
    tool_parity="$(arg tool_parity)"
    tool_baud_rate="$(arg tool_baud_rate)"
    tool_stop_bits="$(arg tool_stop_bits)"
    tool_rx_idle_chars="$(arg tool_rx_idle_chars)"
    tool_tx_idle_chars="$(arg tool_tx_idle_chars)"
    tool_device_name="$(arg tool_device_name)"
    tool_tcp_port="$(arg tool_tcp_port)"
    reverse_ip="$(arg reverse_ip)"
    script_command_port="$(arg script_command_port)"
    reverse_port="$(arg reverse_port)"
    script_sender_port="$(arg script_sender_port)"
    trajectory_port="$(arg trajectory_port)"
  />
  
</robot>
